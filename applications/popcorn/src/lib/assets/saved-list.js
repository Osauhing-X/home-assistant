export function filterSaved(list,savedFilter,savedSort,selectedMonth,folders){const q=savedFilter.toLocaleLowerCase();const filtered=list.filter(x=>(!q||x.title?.toLocaleLowerCase().includes(q)||folders.find(f=>f.id===x.folderId)?.name?.toLocaleLowerCase().includes(q))&&(!selectedMonth||x.date?.startsWith(selectedMonth)));return [...filtered].sort((a,b)=>{if(savedSort==='az')return a.title.localeCompare(b.title);if(savedSort==='za')return b.title.localeCompare(a.title);if(savedSort==='unreleased'){const now=Date.now(),ad=a.date?new Date(a.date).getTime():Infinity,bd=b.date?new Date(b.date).getTime():Infinity;return (ad<now)-(bd<now)||ad-bd}const ad=new Date(a.date||a.createdAt||0),bd=new Date(b.date||b.createdAt||0);return savedSort==='old'?ad-bd:bd-ad})}
