import{randomUUID}from'node:crypto';import{json}from'@sveltejs/kit';import{cameras,save}from'$lib/server/store.js';const clean=x=>({id:String(x.id||randomUUID()).replace(/[^a-zA-Z0-9_-]/g,''),uuid:x.uuid||randomUUID(),name:String(x.name||'Camera').slice(0,80),username:String(x.username||'onvif'),password:String(x.password||''),manufacturer:String(x.manufacturer||'X Platform'),model:String(x.model||'RTSP Bridge'),hq:String(x.hq||''),lq:String(x.lq||''),width:Number(x.width)||1920,height:Number(x.height)||1080,fps:Number(x.fps)||25});export async function GET(){return json({cameras:await cameras()})}export async function POST({request}){const value=clean(await request.json()),all=await cameras();all.push(value);await save(all);return json(value,{status:201})}export async function PUT({request}){const value=clean(await request.json()),all=await cameras(),index=all.findIndex(x=>x.id===value.id);if(index<0)return json({error:'Not found'},{status:404});all[index]=value;await save(all);return json(value)}export async function DELETE({request}){const{id}=await request.json();await save((await cameras()).filter(x=>x.id!==id));return json({ok:true})}
